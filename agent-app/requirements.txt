# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Environment / config
python-dotenv>=1.0.0

# LLM
langchain-anthropic>=0.3.0

# Embeddings — local sentence-transformers, no API key required.
# The model (~90 MB) is downloaded on first use and cached in
# ~/.cache/huggingface.  On Render, add a persistent cache disk or
# accept the one-time download on cold start.
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0

# VectorAI DB — langchain-actian-vectorai pulls actian-vectorai-client as a
# transitive dependency, so we don't need to list it separately.
langchain-actian-vectorai>=1.0.0

# LangGraph agent framework
langgraph>=0.2.0
langchain-core>=0.3.0

# MCP tool integration (Scalekit Virtual MCP Server)
langchain-mcp-adapters>=0.3.0

# Scalekit identity + tool calling.
# scalekit-sdk-python declares grpcio-status<1.67 but works fine with 1.67+
# at runtime (confirmed locally). It is installed with --no-deps in the
# buildCommand to bypass that stale constraint. Its direct deps that aren't
# pulled in transitively by other packages are listed explicitly below.
scalekit-sdk-python==2.12.0
protoc-gen-openapiv2
googleapis-common-protos
deprecation
Faker
PyJWT>=2.12.0

# HTTP (used by Scalekit SDK and FastAPI internally)
httpx>=0.27.0

# protobuf>=6.31.1 is required by actian-vectorai-client at runtime.
# grpcio-status>=1.67.0 works with protobuf 6.x/7.x.  scalekit-sdk-python
# 2.12.0 metadata declares <1.67 but works fine at runtime with 1.67+
# (confirmed in local venv).  scalekit is installed with --no-deps in the
# buildCommand to bypass that stale metadata constraint.
protobuf>=6.31.1
grpcio-status>=1.67.0
